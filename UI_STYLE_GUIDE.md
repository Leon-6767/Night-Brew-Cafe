# Night Brew Cafe · UI 样式指南

本项目的经营 UI 使用 `res://themes/night_brew_theme.tres` 作为统一主题。所有经营页都位于 `CanvasLayer` 的自适应根节点中；世界镜头不会影响卡片、弹窗和导航。

## 色彩

| 用途 | 色值 |
| --- | --- |
| 夜咖啡背景 | `#1B1213` |
| 卡片深棕 | `#482923` |
| 焦糖描边／主操作 | `#BA854F` |
| 奶油文字 | `#FFE8C2` |
| 危险操作 | `#E77D72` |
| R | `#719184` |
| SR | `#4E91C9` |
| SSR | `#8A63C9` |
| SSS | `#D8AD43` |

## 排版与触控

- 普通正文 24–28；卡片名称 30–32；弹窗标题 40–42。
- 卡片之间留 12–16 像素，卡片内部留 16–18 像素。
- 主要操作按钮高度至少 62，独立页面按钮至少 80。
- 采购、员工、人才、菜单、装修均使用滚动内容区，外层始终使用锚点布局，不能用底部绝对坐标。

## 复用场景

- `PanelShell.tscn`：带标题、关闭、内容与底部操作区的标准弹窗外壳。
- `ResourceBar.tscn`：金币、声望、环境、日期资源栏。
- `ShopItemCard.tscn`：食材图标槽、库存条、价格趋势、数量步进与购买。
- `StaffCandidateCard.tscn`：人才候选人详情入口；聘请需要再次确认。
- `StaffManagementCard.tscn`：已聘员工的单一“查看／管理”入口。
- `RecipeCard.tscn`：菜单品项、原料、制作时间、上架／研发操作。
- `FurnitureCard.tscn`：家具／扩建预览与摆放操作。
- `ConfirmDialog.tscn` 与 `ToastMessage.tscn`：危险操作和轻提示。

## 正式美术替换位置

每张卡的左侧图像槽均为 `TextureRect` 或统一色块占位。正式资源到位后，替换为对应的 `Texture2D`，无需改变招聘、采购、摆放或经营函数。建议统一使用 1:1 的 UI 图标（至少 128×128），人物头像用 3:4 竖图（至少 256×340），家具缩略图用 2:1 透明 PNG（至少 256×128）。

品质边框继续由卡片脚本根据 R／SR／SSR／SSS 决定；正式边框图片可在保留该颜色作为后备色的前提下替换。
